'use strict';

angular.module('pixie.admin').controller('OrderController', ['$scope', '$rootScope', '$state', '$mdDialog', 'utils', 'order', function($scope, $rootScope, $state, $mdDialog, utils, order) {
    /*$scope.stats = {};*/
    alert(1);
}]);
